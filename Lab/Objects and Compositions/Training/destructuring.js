const person = {
    name:'Nadia',
    age:18,
    university:'Sofia University'
}
let {name,age} = person//we must set the same names as the keys above
//destructuring, the object don't change his structure
console.log(name)
console.log(age)
